// Copyright (c) Meta Platforms, Inc. and affiliates.

/* Auto-generated by export_to_c.py on 2026-08-24T10:39:07.578872 */
/* Neural Network scorer for num16 (sigmoid output, multi-target regression) */

#ifndef OPENZL_COMPRESS_SELECTORS_TRANSFORMER_SCORE_NUMERIC16_H
#define OPENZL_COMPRESS_SELECTORS_TRANSFORMER_SCORE_NUMERIC16_H

#include "openzl/compress/selectors/transformer/generic_numeric_ops.h"
#include "openzl/compress/selectors/transformer/numeric_features.h"
#include "openzl/shared/portability.h"

ZL_BEGIN_C_DECLS

#define TRS_SCORE_NUM16_N_OPERATIONS 12

/* Operation identifiers, indexed by output neuron. */
extern const TRS_GenericNumericOpId TRS_score_num16_operation_ids[];

/*
 * Score each operation for a num16 stream.
 * Writes 12 scores in [0, 1] to scores_out.
 * Returns index of highest-scoring operation.
 */
int TRS_score_num16_operations(
        const TRS_NumericFeatures* f,
        float scores_out[TRS_SCORE_NUM16_N_OPERATIONS]);

/*
 * Score each operation for a num16 stream and optionally expose raw logits.
 * Writes 12 sigmoid scores in [0, 1] to scores_out.
 * When logits_out is non-NULL, writes the pre-sigmoid outputs there.
 * Returns index of highest-scoring operation.
 */
int TRS_score_num16_operations_with_logits(
        const TRS_NumericFeatures* f,
        float scores_out[TRS_SCORE_NUM16_N_OPERATIONS],
        float logits_out[TRS_SCORE_NUM16_N_OPERATIONS]);

ZL_END_C_DECLS

#endif /* OPENZL_COMPRESS_SELECTORS_TRANSFORMER_SCORE_NUMERIC16_H */
